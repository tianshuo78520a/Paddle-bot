name: Coverage

on:
  push:
    branches: [ "master" ]
 
jobs:
  test:
    runs-on: [ CPU ]
    steps:
    - name: Upload coverage to Codecov
      uses: actions/upload-artifact@v2
      with:
        name: my-artifact
        path: /home/paddle/actions-runner/_work/Paddle-bot/Paddle-bot/index.html
